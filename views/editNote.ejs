<%- include ('./partials/header.ejs') -%> 
<div class="container">
    <nav class="navbar">
        <a href="/" class="option">My Note's</a>
        <div class="options">
            <% if (user) { %>
                <a href="/user/session" class="option"><%= user.username %> </a>
            <% } %>
            <a href="/user/session" class="option">Create Note</a>
            <a href="/user/logout" class="option">Logout</a>
        </div>
    </nav>
    <main class="main">
        <h3>Edit Note</h3>
        <form action="/notes/update/<%= note._id %> " method="post">
            <input type="text" name="title" id="title" value="<%= note.title %> ">
            <input type="text" name="description" id="description" value="<%= note.description %> ">
            <button type="submit" class="btn">Update</button>
        </form>
    </main>
</div>

<%- include ('./partials/footer.ejs') -%> 